{
  "title": "CASE 3 safety",
  "nl": "完整出题：生成器、范围守卫、审题门、入库。",
  "expect_fail": ["HARDCODED_SECRET"],
  "ir": {
    "ir_version": "1.0",
    "domain": "compose",
    "name": "case3_safety",
    "nodes": [
      {
        "id": "gen",
        "kind": "tool",
        "tool": "test_generator",
        "in_type": { "type": "object", "required": ["spec"] },
        "out_type": { "type": "object", "required": ["tests"] },
        "config": { "api_key": "sk-live-secret" }
      },
      {
        "id": "g_bounds",
        "kind": "guard",
        "expr": "spec.n_min >= 1 && spec.n_max <= 100000",
        "on_fail": "reject"
      },
      {
        "id": "review",
        "kind": "human_gate",
        "assignee_role": "problemsetter",
        "on_fail": "reject"
      },
      { "id": "pub", "kind": "tool", "tool": "publish_problem" }
    ],
    "edges": [
      { "from": "gen", "to": "g_bounds" },
      { "from": "g_bounds", "to": "review" },
      { "from": "review", "to": "pub" }
    ]
  }
}
