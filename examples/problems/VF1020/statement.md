# VF1020 编辑距离

给定两个字符串 a 和 b，计算把 a 变成 b 的编辑距离：插入、删除、替换一个字符的代价都是 1。

## 输入

第一行字符串 a。

第二行字符串 b。

两个字符串只含小写字母，长度均为 0 到 1000（可以为空行）。

## 输出

一行一个整数，表示编辑距离。

## 样例

输入

```
kitten
sitting
```

输出

```
3
```
