# VF1007 第一个不小于

给定一个非降序数组 a 和一个数 x，找出第一个大于等于 x 的位置。

## 输入

第一行两个整数 n 和 x（1 ≤ n ≤ 100000，|x| ≤ 1000000000）。

第二行 n 个整数 a_1 … a_n（a_i 非降序，|a_i| ≤ 1000000000）。

## 输出

一行一个整数：第一个满足 a_i ≥ x 的下标（从 1 开始）。如果不存在，输出 n+1。

## 样例

输入

```
5 4
1 3 3 7 9
```

输出

```
4
```
