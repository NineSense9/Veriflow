# Veriflow Training Desk Web

**Goal:** Next.js 训练站：登录、训练桌、题库、做题台、提交状态。视觉为夜场做题桌（暖纸题面 + 冷色终端）。

**Stack:** Next.js App Router、@monaco-editor/react、自定义 CSS（不用 shadcn / Inter）。`/api/*` rewrite 到 FastAPI。

Pages: `/login` `/` `/problems` `/problems/[id]` `/status`
