# 演示视频脚本（约 3 分钟）

不要做成刷题流水。结构按痛点 → 出题编译 → 做题对抗 → 对照数字。

## 0:00–0:20 痛点

画面：普通 OJ 的 WA on test 3；Chat 窗口直接贴题解。  
旁白：过样例不算完。模型不能既当选手又当裁判。

## 0:20–1:20 出题编译（方案二）

1. 打开 http://116.62.5.67/compose  
2. 点「缺审题门」：图上 `publish_problem` 变红，右侧 `MISSING_HUMAN_GATE`，点入库失败。  
3. 返回点「完整出题图」：生成器 → 守卫 → 象牙色审题门 → 发布。  
4. 审题通过，入库。  

旁白：自然语言先编译成带类型的图。缺人工门，编译期就拦住。

## 1:20–2:20 做题对抗（方案一）

1. 打开 VF1001，空 `print()` 提交 → WA，三列反例。  
2. 点教练：只出现一个问题，没有代码。  
3. 打开对拍，开拍，第 1 轮停在反例。  
4. 提交正确求和 → AC，顶栏可见 docker。  

旁白：隐藏测资和对拍由环境执行。教练只准追问。

## 2:20–2:50 对照

打开「报告」页或展示 `python scripts/eval.py`：  
样例 AC、完整路径 WA（hidden）、变异 3/3 杀死、缺审题门召回。  

旁白：数字可复现，不是手填。

## 2:50–3:00 收束

一句话：大模型当编译器和攻击者，Docker 当裁判。  
出片：演示地址与仓库地址。

## 录制注意

- 用公网地址，不要用 localhost。  
- 不要出现 `.env` 和 Key。  
- 分辨率 1920×1080，浏览器全屏，隐藏书签栏。  
